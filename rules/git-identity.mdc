---
description: Git 提交与推送不得暴露 Cursor、AI 模型或其他用户身份
alwaysApply: true
---

# Git 提交身份规范

执行 `git commit`、`git push` 或相关操作时，**严禁**在提交记录中留下 Cursor、AI 助手或模型相关信息。

## 禁止事项

- **不得修改提交者身份**：禁止使用 `--author`、`GIT_AUTHOR_NAME`、`GIT_AUTHOR_EMAIL`、`GIT_COMMITTER_NAME`、`GIT_COMMITTER_EMAIL` 等将作者设为 Cursor、Composer、Claude、GPT、Copilot、Agent 或任何非仓库主人的身份。
- **不得添加 AI 署名 trailer**：禁止在 commit message 中加入 `Co-authored-by:`、`Signed-off-by:`、`Assisted-by:` 等指向 Cursor 或 AI 模型的行。
- **不得在 message 中提及工具或模型**：提交说明里不得出现 Cursor、Composer、Claude、GPT、Qoder、Agent、LLM、大模型、AI 生成 等字样。
- **不得代替其他用户提交**：只使用仓库主人已有的 git 用户配置；若身份未配置，应提示用户自行设置，而不是用 Agent 默认身份代填。

## 正确做法

- 提交说明只描述**代码/文档变更本身**，聚焦 why，不写 who/what tool 做的。
- 执行 commit 前可用 `git log -1 --format='%an <%ae>'` 确认作者是否为预期用户；若不是，停止提交并告知用户。
- 仅在用户**明确要求**时才 push；push 前不修改 remote、不 force push。
- 首次在本仓库提交前，运行 `bash hooks/setup.sh` 启用 `hooks/commit-msg` 硬性校验。

## 提交说明示例

```
# ❌ 错误
feat: add README (generated by Cursor with Claude)

Co-authored-by: Cursor <noreply@cursor.com>

# ✅ 正确
docs: add project README for skill sharing
```
